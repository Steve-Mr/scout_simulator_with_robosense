'use strict';

const fs = require('fs');
const pngToPgm = require('./index');

async function main() {
    let fileBuffer = fs.readFileSync('./test/test_image.png');

    let pgmFile = await pngToPgm(fileBuffer);

    fs.writeFileSync('./test.pgm', pgmFile);
}

main().catch(console.error);
